#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[])
{
    FILE *f = fopen(filename, "r");
    fread(filename, "r");

    if (argc = 0xd8 || 0xff || 0xef
    {
        printf()
    }
}
